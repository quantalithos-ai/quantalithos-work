[package]
name = "work-api"
version = "0.1.0"
edition = "2024"

[lib]
name = "work_api"
path = "src/lib.rs"

[dependencies]
core-contracts.workspace = true
work-application = { path = "../application" }
work-contracts = { path = "../contracts" }
work-infra = { path = "../infra" }
